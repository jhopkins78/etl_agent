# Database connection details
DB_HOST=localhost
DB_PORT=5432
DB_NAME=etl_database
DB_USER=postgres
DB_PASSWORD=your_password_here

# Optional: Additional configuration
# BATCH_SIZE=1000  # Number of rows to load in a single batch
# MAX_RETRIES=3    # Maximum number of retries for failed loads
